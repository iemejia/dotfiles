# Cache passphrase for 1 hour (3600s), max 4 hours (14400s)
default-cache-ttl 3600
max-cache-ttl 14400

# SSH key cache (if using gpg-agent for SSH)
default-cache-ttl-ssh 3600
max-cache-ttl-ssh 14400

# Allow loopback pinentry (needed for some terminal workflows)
allow-loopback-pinentry
